package
{
   import flash.display.BitmapData;
   
   public dynamic class packMentalityBitmap extends BitmapData
   {
       
      
      public function packMentalityBitmap(param1:int = 64, param2:int = 28)
      {
         super(param1,param2);
      }
   }
}
